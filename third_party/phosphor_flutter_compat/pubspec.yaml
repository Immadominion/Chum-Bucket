name: phosphor_flutter
description: Local compatibility shim for the subset of Phosphor icons used by chumbucket.
version: 2.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
